#include "main.h"

/**
 * mul - a function that multiplies two integers
 * @a: first character
 * @b: second character
 * Return: result
 */
int mul(int a, int b)
{
		int total;

		total = a * b;
		return (total);
}
